# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyStoreSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for MyStoreSwift
  pod "Mobile-Buy-SDK"
  pod "Apollo"
  pod "Alamofire"
  pod "SwiftyJSON"
  pod 'AlamofireImage'

  target 'MyStoreSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyStoreSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
